/**
 * @deprecated Prefer `liveFeed` — multi-venue aggregator (Binance / Bybit / OKX).
 * Kept as a compatibility alias so older imports keep working.
 */
export { liveFeed as binanceFeed, LiveFeed as BinanceFeed } from './liveFeed';
